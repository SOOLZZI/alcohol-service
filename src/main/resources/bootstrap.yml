spring:
  application:
    name: alcohol-service
  cloud:
    kubernetes:
      config:
        enabled: true
        name: alcohol-service-config
      reload:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always